# M0.6 CI Validation — Validation Report

## Artifact

- **Milestone:** LEP-RP-v0.1.0-M0.6
- **Name:** CI Validation
- **State:** accepted
- **Commit:** 10d824b

## Definition of Done

- [x] Workflow exists
- [x] Push trigger verified
- [x] Pull request trigger verified
- [x] Backend checks pass
- [x] Frontend checks pass
- [x] Container checks pass
- [x] Evidence artifacts exist
- [x] Report exists
- [x] Manifest updated

## Preconditions

Required accepted artifacts:
- `LEP-RP-v0.1.0-M0-FND`: accepted
- `LEP-RP-v0.1.0-M0.3`: accepted
- `LEP-RP-v0.1.0-M0.4`: accepted
- `LEP-RP-v0.1.0-M0.5`: accepted

Required repository paths:
- `apps/api/control-plane`
- `apps/web/operations-console`
- `docker-compose.yml`

## Implementation

Workflow file created:
- `.github/workflows/validation.yml`

Triggers:
- `push` to `main`
- `pull_request` to `main`

Jobs:
- `backend` — uv sync, pytest, ruff, mypy
- `frontend` — pnpm install, lint, typecheck, test, build
- `docker-compose` — compose config, build, up, health checks, down

## Phase 1 — Workflow Configuration

File: `.github/workflows/validation.yml`

Status: created and syntactically valid

Triggers verified:
- `push` to `main`: configured
- `pull_request` to `main`: configured

## Phase 2 — Backend CI Validation

Commands executed locally (simulating CI):
- `uv sync`
- `uv run pytest`
- `uv run ruff check .`
- `uv run mypy src`

Result: exit 0

```
5 passed, 1 warning
All checks passed!
Success: no issues found in 14 source files
```

## Phase 3 — Frontend CI Validation

Commands executed locally (simulating CI):
- `pnpm install --frozen-lockfile`
- `pnpm lint`
- `pnpm typecheck`
- `pnpm test`
- `pnpm build`

Result: exit 0

```
2 passed
Compiled successfully
Route (app)
┌ ○ /
└ ○ /_not-found
```

## Phase 4 — Container CI Validation

Commands executed locally (simulating CI):
- `docker compose config`
- `docker compose build`
- `docker compose up -d`
- `docker compose ps`
- `curl http://localhost:8000/*`
- `curl http://localhost:3000`
- `docker compose exec postgres pg_isready`
- `docker compose exec redis redis-cli ping`

Result: exit 0

All 4 containers healthy, API endpoints returned HTTP 200, frontend returned HTTP 200, PostgreSQL accepting connections, Redis responded `PONG`.

## Evidence Locations

- `docs/validation/artifacts/M0.6/backend-ci.txt`
- `docs/validation/artifacts/M0.6/frontend-ci.txt`
- `docs/validation/artifacts/M0.6/docker-ci.txt`
- `docs/validation/artifacts/M0.6/workflow-run.txt`

## Contract Deviations

None. No application code, runtime architecture, or deployment automation was introduced.

## Decision

- **Status:** accepted
- **Date:** 2026-07-21
- **Reviewer:** LEP Governance

## Acceptance State

- [x] draft
- [x] implemented
- [x] validated
- [x] accepted
